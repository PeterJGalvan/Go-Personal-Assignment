package algo

func readInputs(fileName string) ([]float32, error) {
	/*
		Write code to read in an input file and format
		the values into []float32
	*/

	return nil, nil
}

func countAverages(fileName string) ([]float32, error) {
	/*
		// call readInputs() to get values
		Fill out solution here.
	*/

	return nil, nil
}
